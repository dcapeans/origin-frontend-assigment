import styled from "styled-components";

export default function Header(): JSX.Element {
  return <HeaderContainer>origin</HeaderContainer>;
}

const HeaderContainer = styled.div`
  width: 100vw;
  height: 80px;
  display: flex;
  align-items: center;
  font-size: 36px;
  padding-left: 50px;
  background-color: #fff;
`;
